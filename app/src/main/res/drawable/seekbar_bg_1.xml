<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 背景效果 -->

    <item android:id="@android:id/background">
        <shape>
            <!-- angle 是角度 -->
            <gradient
                android:angle="90"
                android:centerColor="@color/mine_menu_deepgray"
                android:centerY="0.75"
                android:endColor="@color/mine_menu_deepgray"
                android:startColor="@color/mine_menu_deepgray" />
        </shape>
    </item>

    <!-- 第一进度条 -->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="90"
                    android:centerColor="@color/login_password"
                    android:centerY="0.75"
                    android:endColor="@color/login_password"
                    android:startColor="@color/login_password" />
            </shape>
        </clip>
    </item>

</layer-list>